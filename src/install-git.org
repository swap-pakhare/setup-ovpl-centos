#+TITLE: Install git from sources
#+AUTHOR: VLEAD
#+DATE: [2017-11-07 Tue]
#+SETUPFILE: ./org-templates/level-0.org
#+TAGS: boilerplate(b)
#+EXCLUDE_TAGS: boilerplate
#+OPTIONS: ^:nil

* Introduction
  This document lists the steps required to install git from
  sources on centos 6.9.

* Install Git

#+BEGIN_SRC sh :tangle scripts/install-git.sh

#!/bin/bash
# Script to install git
# Installs: Dependencies: curl-devel expat-devel 
# gettext-devel openssl-devel zlib-devel gcc 
# perl-ExtUtils-MakeMaker

yum update -y

yum install curl-devel expat-devel gettext-devel openssl-devel zlib-devel -y
yum install  gcc perl-ExtUtils-MakeMaker -y

yum remove git

cd /usr/src

wget https://www.kernel.org/pub/software/scm/git/git-2.7.4.tar.gz
tar xzf git-2.7.4.tar.gz
cd git-2.7.4
make prefix=/usr/local/git all
make prefix=/usr/local/git install
echo 'export PATH=$PATH:/usr/local/git/bin' >> /etc/bashrc
export PATH=$PATH:/usr/local/git/bin
exit 0

#+END_SRC
